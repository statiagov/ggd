$('##{dom_id(@attachment)}').slideUp(250);

- if called_from_landing_page?(:accounts)
  = refresh_sidebar_for(:accounts, :show, :summary)
- elsif called_from_landing_page?(:inspections)
  = refresh_sidebar_for(:inspections, :show, :summary)

- if (params['controller'] == 'inspections') and (template = template_for_current_view).present?
  = render(template, :inspection => inspection)
- else
  = render 'inspections/index_brief', :inspection => inspection
